# LinkSelf — プロジェクト概要

主権ある個人のための**サーバー不要の P2P インフラ**。DID（did:key）+ libp2p DHT + Store-and-Forward + E2E 暗号化。
ライセンス: Apache-2.0。

## リポジトリ構成

```
link-self/
├── core/                       # Go コア（ライブラリ + daemon）
│   ├── go.mod                  # module github.com/SeijiShii/link-self/core
│   ├── internal/               # 実装詳細（外部 import 不可）
│   │   ├── did/                # did:key 生成・検証（Ed25519）
│   │   ├── dht/                # DHT Provide/Find ラップ
│   │   ├── auth/               # チャレンジ・レスポンス認証
│   │   ├── node/               # ノード（Host + DHT + Auth + Store-and-Forward）
│   │   ├── storeforward/       # メッセージキュー・オンライン検知時送信
│   │   ├── group/              # グループ（メンバー DID 集合）・ストア
│   │   ├── devicesync/         # DeviceSync（同一DID間の透過的DB同期）
│   │   ├── groupshare/         # GroupShare（異なるDID間のChannel型データ共有）
│   │   └── syncdb/             # 旧同期 DB 層（廃止予定 → devicesync + groupshare）
│   ├── pkg/linkself/           # 公開 API（Client インターフェース、型定義）
│   ├── cmd/linkself-daemon/    # JSON-RPC daemon（Electron 等から子プロセスで利用）
│   └── test/integration/       # 多ノード統合テスト
├── chat-client/                # サンプルチャットアプリ（Electron + React + TypeScript + Vite）
│   ├── src/                    # main / preload / renderer（React コンポーネント・hooks）
│   └── docs/                   # 実装計画・テストガイド等
├── docs/                       # 全体設計・計画ドキュメント（日英）
└── TODO.md                     # リポジトリ全体の中央 TODO
```

## ロードマップ

| Phase | 内容 | 状態 |
|-------|------|------|
| Phase 1 | コアロジック（DID・DHT・認証・Store-and-Forward）の実装、ローカル多ノードテスト | 完了 |
| Phase 2 | インフラモジュール整備。LinkSelf をインフラとして使う本格サンプルアプリ（チャット+ファイル共有）。ライブラリ利用の道筋整備 | 進行中 |
| Phase 3 | プラットフォーム展開（PC、Android、iOS） | 未着手 |

## 技術スタック

- **コア言語**: Go（core/）
- **DID**: `did:key:`（Ed25519）。libp2p PeerID と相互変換可能
- **DHT**: 常に公開 DHT（`/ipfs`）。FindPeer(DIDToPeerID(did)) で検索
- **メッセージプロトコル**: `/linkself/msg/1.0.0`（4 バイト BigEndian 長 + 本体）
- **チャットクライアント**: Electron + React + TypeScript + Vite
- **daemon 通信**: JSON-RPC（chat-client → linkself-daemon）

## ビルド・テスト

```bash
cd core
go build ./...
go test ./...              # 単体テスト + 統合テスト
go test ./test/integration/...  # 多ノード統合テストのみ
```

## 公開 API（pkg/linkself）

主要インターフェース: `Client`
- `NewClient(Config)` → `Start()` / `Stop()`
- `SendMessage(ctx, targetDID, payload)` / `Connect(ctx, targetDID)`
- `SetOnMessage(handler)`
- `DeviceDB()` — 同一DID間の透過的DB同期（DeviceSync）
- `GroupShare()` — 異なるDID間のChannel型データ共有（GroupShare）
- 将来: `SendToGroup` / `ConnectToGroup` への移行を計画

## 次の作業候補

TODO.md セクション 6 を参照。主な項目:
- チャットクライアントを linkself-data-persistence-plan.md に沿って作り変える（LinkSelf データルート/DID 空間/アプリID）
- Core API: SendToGroup / ConnectToGroup への移行
- ファイル共有スコープの検討・実装

## 重要な設計方針

- **グループ**: メッセージ・接続はグループ単位。1対1 は 2 人グループのケース
- **Store-and-Forward**: オフライン時はローカルキューに保持、相手オンライン検知後に送信
- **DID 空間**: Phase 2 でデータ保存を「LinkSelf データ > DID 空間 > アプリごとのデータ」に移行予定
- **daemon**: `pkg/linkself` のみに依存（internal 直接依存なし）
- **DeviceSync / GroupShare 二層アーキテクチャ**: 同一DID間（DeviceSync: 透過的全DB同期）と異なるDID間（GroupShare: Channel定義+権限制御）でデータ共有を分離。旧syncdbを置換

## 主要ドキュメント

- docs/README.ja.md — 詳細説明（シナリオ・技術・FAQ）
- docs/phase1-design.md — Phase 1 設計（実装済み）
- docs/group-concept.md — グループの概念
- docs/sync-db-plan.md — DeviceSync / GroupShare 二層アーキテクチャ設計
- docs/sample-chat-app-plan.md — サンプルアプリ計画
- docs/linkself-data-persistence-plan.md — Phase 2 永続化・DID 空間方針
- docs/using-linkself-as-library.md — ライブラリ利用の道筋
